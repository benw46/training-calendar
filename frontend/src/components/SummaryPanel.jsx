export default function SummaryPanel({ workoutsByDate, days }) {
  // Populated in Stage 7 — renders placeholder for now
  return (
    <div className="summary-panel">
      <div className="summary-title">Summary</div>
    </div>
  )
}
